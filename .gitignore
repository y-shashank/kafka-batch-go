# Binaries (local builds / itest harnesses)
bin/
!compat/ruby/bin/
/kbatch

# Test / coverage
*.test
coverage.out

# Ruby compat runtime logs
compat/ruby/log/

# Local kafka-batch gem symlink (CI checks out to this path)
/kafka-batch
